<!DOCTYPE html>
<html>
<head>
    <title>Multi-Language Example</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
        }

        .container {
            width: 80%;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border: 1px solid #ccc;
            box-shadow: 0px 0px 5px #aaa;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Hello from HTML & CSS!</h1>
        <p>This is a simple web page that combines HTML, CSS, and JavaScript.</p>

        <script>
            // JavaScript code
            var greeting = "Hello from JavaScript!";
            console.log(greeting);
        </script>

        <!-- Python code (not executable in the browser, for demonstration purposes only) -->
        <script type="text/python">
            # Python code (this will not run in the browser)
            def greet():
                return "Hello from Python!"

            python_greeting = greet()
            console.log(python_greeting)  // This won't work in JavaScript, just for demonstration.
        </script>

        <!-- Java code (not executable in the browser, for demonstration purposes only) -->
        <script type="text/java">
            // Java code (this will not run in the browser)
            public class HelloWorld {
                public static void main(String[] args) {
                    System.out.println("Hello from Java!");
                }
            }
        </script>
    </div>
</body>
</html>
